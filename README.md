# great-southern-websites.github.io

The organisation Pages site for Great-Southern-Websites. It exists for one reason: the
custom domain set on it (view.greatsouthern.website) is inherited by every project site in
the organisation, so each built site is reachable at view.greatsouthern.website/<repo>/
before the customer connects their own domain. The root redirects to the main site.
